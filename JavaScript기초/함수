
//함수 선언
function doSomething() {
    console.log('hello');
}

이 상태에선 아무것도 출력이 안됨, 왜냐하면 선언만 해놓은 상태이기 때문 ㅋㅋ

//함수호출
doSomething();

doSomething : 함수의 이름, 우리가 원하는데로 지정 가능!
() : 괄호안에는 우리가 함수에 어떤값을 전달 받아 올건지, 전달받을 인자를 정의하는 부분
{} : 실제로 코드안에서 어떤 코드가 등장하는지 ㅋㅋ, 코드를 작성하는 부분


//return
function add(a, b) {
    const sum = a + b;
    return sum;
}

const result = add(1, 2);
console.log(result);